{
  "name": "carlosdscunha-profile",
  "version": "1.0.0",
  "description": "Gerador isométrico e estatísticas neon para GitHub",
  "main": "scripts/generate-isometric.js",
  "scripts": {
    "generate:isometric": "ts-node scripts/generate-isometric.ts --user=carlosdscunha --out=assets/contrib-city.svg",
    "generate:stats": "ts-node scripts/gen-stats.ts",
    "generate:all": "npm run generate:isometric && npm run generate:stats"
  },
  "license": "MIT",
  "dependencies": {
    "@octokit/graphql": "^9.0.3",
    "dotenv": "^17.2.3",
    "minimist": "^1.2.8"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.5",
    "@types/node": "^25.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}